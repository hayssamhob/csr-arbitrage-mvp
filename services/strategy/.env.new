# Strategy Engine Configuration
# Copy to .env and adjust values

# Execution mode: off | paper | live
# Default: off (monitoring only, no trades executed)
# paper: simulate trades without real money
# live: execute real trades (requires additional API keys)
EXECUTION_MODE=off

# LBank Gateway WebSocket URL (internal service)
LBANK_GATEWAY_WS_URL=ws://localhost:8080

# Uniswap Quote Service URL (internal service)
UNISWAP_QUOTE_URL=http://localhost:3002

# Symbol to monitor (must match LBank gateway subscription)
SYMBOL=csr_usdt

# Quote size for Uniswap quotes (in USDT)
QUOTE_SIZE_USDT=1000

# Minimum edge threshold in basis points (1 bps = 0.01%)
# Strategy will only flag "would_trade=true" if edge >= threshold
MIN_EDGE_BPS=50

# Estimated trading costs in basis points
# Includes: LP fees (~30bps for 0.3% pool), gas, slippage buffer
ESTIMATED_COST_BPS=30

# Maximum trade size in USDT (for suggested_size calculation)
MAX_TRADE_SIZE_USDT=5000

# Polling interval for Uniswap quotes (milliseconds)
UNISWAP_POLL_INTERVAL_MS=10000

# Staleness threshold in seconds
MAX_STALENESS_SECONDS=30

# HTTP port for health endpoints
HTTP_PORT=3003

# Log level: debug, info, warn, error
LOG_LEVEL=info
